# Solution

<!-- checkpoint: configuration-ready -->

Create the configuration file with `enabled=true`.

<!-- checkpoint: derived-state-ready -->

After restoring the configuration, create the state file with `service=enabled`.
