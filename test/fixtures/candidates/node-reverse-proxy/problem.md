# Repair the reverse proxy

The application is healthy on `app`, but requests from `client` to `http://proxy:8080` fail. Repair the systemd-managed reverse proxy without changing the application service or its listening port.
