openapi: "3.0.3"
info:
  title: Breakfix API
  version: "1.0.0"
  description: SRE/DevOps operations scenario practice platform
servers:
  - url: http://localhost:9090/api
    description: Local dev

paths:
  /documentation/links:
    get:
      operationId: listDocumentationLinks
      summary: List every documentation link in creation order (public read)
      description: The shared list behind the documentation aggregation page; admin writes go through /admin/documentation/links.
      tags: [documentation]
      responses:
        "200":
          description: All documentation links, oldest first
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentationLinkList"
        "503":
          $ref: "#/components/responses/Error"

  /playground:
    get:
      operationId: getPlayground
      summary: Report the user's playground session (JWT)
      description: One playground per user, bound to the user alone and carried across the whole site.
      tags: [playground]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Current session state; state is none when no environment exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlaygroundEnvironment"
        "401":
          $ref: "#/components/responses/Error"
    post:
      operationId: createPlayground
      summary: Create or adopt the user's playground (JWT)
      description: Idempotent find-or-create; the response returns as soon as the environment is named and readiness arrives through polling.
      tags: [playground]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Session state after creation or adoption
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlaygroundEnvironment"
        "401":
          $ref: "#/components/responses/Error"
        "429":
          description: The site-wide active playground fleet is at the configured capacity; retry after a session closes
    delete:
      operationId: closePlayground
      summary: Close the playground and release its resources (JWT)
      description: Closing without an active session is a successful no-op.
      tags: [playground]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Session closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlaygroundCloseResponse"
        "401":
          $ref: "#/components/responses/Error"

  /playground/reset:
    post:
      operationId: resetPlayground
      summary: Wipe the playground to a fresh state (JWT)
      description: A Ready-session verb; an in-flight reset stays idempotent and returns the current session state.
      tags: [playground]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Reset requested; the session returns to creating
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlaygroundEnvironment"
        "401":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"

  /playground/terminal-ticket:
    post:
      operationId: createPlaygroundTerminalTicket
      summary: Create a one-time ticket for the playground terminal WebSocket (JWT)
      tags: [playground]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TerminalTicketRequest"
      responses:
        "200":
          description: Short-lived one-time terminal ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TerminalTicketResponse"
        "401":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"

  /auth/register:
    post:
      operationId: register
      summary: Register a new user; the first registered user becomes admin
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: User registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterResponse"
        "400":
          $ref: "#/components/responses/Error"
        "403":
          description: Registration is disabled by configuration

  /admin/users:
    get:
      operationId: listAdminUsers
      summary: List user accounts without credentials (admin only)
      tags: [admin]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: All user accounts, oldest first
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUserList"
        "401":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"

  /admin/users/{id}/totp-reset:
    post:
      operationId: resetAdminUserTOTP
      summary: Replace a user's TOTP secret after the caller confirms their own password (admin only)
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminTOTPResetRequest"
      responses:
        "200":
          description: New TOTP secret, shown exactly once
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RegisterResponse"
        "401":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"

  /auth/login:
    post:
      operationId: login
      summary: Login with password + TOTP
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Login successful, returns JWT token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "401":
          $ref: "#/components/responses/Error"

  /admin/audit:
    get:
      operationId: listAdminAudit
      summary: Page the append-only human action audit ledger (admin only)
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: cursor
          in: query
          description: Opaque cursor from a previous page
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: action
          in: query
          description: Filter by exact administrative action name
          schema:
            type: string
        - name: user_id
          in: query
          description: Filter by the acting user identifier
          schema:
            type: string
      responses:
        "200":
          description: One page of human actions, newest first
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminAuditPage"
        "400":
          $ref: "#/components/responses/Error"
        "401":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"

  /admin/runnable-actions:
    get:
      operationId: listAdminRunnableActions
      summary: Observe the runnable action queue with backlog summary and flags (admin only)
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: state
          in: query
          description: Filter items by queue state; the summary always covers the whole queue
          schema:
            type: string
            enum: [queued, running, completed, failed]
        - name: phase
          in: query
          description: Filter items by action phase
          schema:
            type: string
            enum: [materialize-artifact, verify]
      responses:
        "200":
          description: Queue summary and items
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminRunnableActionPage"
        "400":
          $ref: "#/components/responses/Error"
        "401":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"

  /admin/runnable-reaps:
    get:
      operationId: listAdminRunnableReaps
      summary: Observe the durable reap queue behind environment teardown (admin only)
      description: Reap rows newest first, capped at 100; state/attempt/last_error expose stuck teardowns without database access.
      tags: [admin]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Reap queue observations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminRunnableReapList"
        "401":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"

  /admin/environments:
    get:
      operationId: listAdminEnvironments
      summary: List every RuntimeEnvironment across users with lifecycle state (admin only)
      tags: [admin]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: All runtime environments, newest first
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminEnvironmentList"
        "401":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"

  /admin/environments/{name}/release:
    post:
      operationId: releaseAdminEnvironment
      summary: Request an administrative environment release via the existing drain path (admin only)
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Release requested; the controller drain path takes over
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminEnvironmentRelease"
        "401":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"

  /admin/documentation/links:
    post:
      operationId: createAdminDocumentationLink
      summary: Add one documentation link to the shared aggregation list (admin only)
      tags: [admin]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminDocumentationLinkInput"
      responses:
        "200":
          description: The created link with its server-generated key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentationLink"
        "400":
          $ref: "#/components/responses/Error"
        "401":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"

  /admin/documentation/links/{key}:
    patch:
      operationId: updateAdminDocumentationLink
      summary: Replace one documentation link's title, URL, and embed flag (admin only)
      description: The key never changes; saved references to the link stay valid across renames.
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminDocumentationLinkInput"
      responses:
        "200":
          description: The updated link
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentationLink"
        "400":
          $ref: "#/components/responses/Error"
        "401":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"
    delete:
      operationId: deleteAdminDocumentationLink
      summary: Remove one documentation link from the shared list (admin only)
      tags: [admin]
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The removed link's key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminDocumentationLinkDeletion"
        "401":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"

  /admin/system:
    get:
      operationId: getAdminSystem
      summary: Server build, catalog integrity, and background service registry (admin only)
      tags: [admin]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: System status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminSystemStatus"
        "401":
          $ref: "#/components/responses/Error"
        "403":
          $ref: "#/components/responses/Error"

  /me/space:
    get:
      operationId: getMySpace
      summary: Get the authenticated user's learning space
      tags: [users]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Learning history, active environments, and authoring summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MySpace"
        "401":
          $ref: "#/components/responses/Error"

  /me/space/learning:
    get:
      operationId: getMySpaceLearning
      summary: Page durable learning history for the authenticated user
      tags: [users]
      security:
        - bearerAuth: []
      parameters:
        - name: cursor
          in: query
          description: Opaque cursor from a previous page
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: state
          in: query
          description: Filter attempts by their current learning state
          schema:
            type: string
            enum: [active, completed, ended]
        - name: runtime
          in: query
          description: Filter attempts by environment runtime
          schema:
            type: string
            enum: [node, k8s]
      responses:
        "200":
          description: One page of learning history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MySpaceLearningPage"
        "401":
          $ref: "#/components/responses/Error"

  /operations/scenarios:
    get:
      operationId: listScenarios
      summary: List operations scenarios
      tags: [operations]
      responses:
        "200":
          description: Scenario list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScenarioList"

  /operations/scenarios/{id}/start:
    post:
      operationId: startScenario
      summary: Start or resume a scenario environment
      tags: [operations]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Environment ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartResponse"
        "404":
          $ref: "#/components/responses/Error"

  /operations/scenarios/{id}/content:
    get:
      operationId: getScenarioContent
      summary: Get scenario details and any available learning aids
      tags: [operations]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Scenario content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScenarioContent"
        "404":
          $ref: "#/components/responses/Error"

  /operations/scenarios/{id}/progress:
    get:
      operationId: getScenarioProgress
      summary: Read the controller-owned current scenario checkpoint snapshot
      tags: [operations]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Current checkpoint results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScenarioProgress"
        "404":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"

  /operations/scenarios/{id}/assistant:
    get:
      operationId: getScenarioAssistant
      summary: Read or create the assistant conversation for the active scenario environment
      tags: [assistant]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Environment-scoped assistant conversation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssistantConversation"
        "404":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"

  /operations/scenarios/{id}/assistant/messages:
    post:
      operationId: sendScenarioAssistantMessage
      summary: Send a message to the active scenario assistant and receive SSE events
      tags: [assistant]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssistantMessageRequest"
      responses:
        "200":
          description: Server-sent assistant status, tool, delta, complete, and error events
          content:
            text/event-stream:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"

  /operations/scenarios/{id}/reset:
    post:
      operationId: resetScenario
      summary: Reset scenario to initial state
      tags: [operations]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Scenario reset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResetResponse"
        "404":
          $ref: "#/components/responses/Error"

  /operations/scenarios/{id}/stop:
    post:
      operationId: stopScenario
      summary: Stop the current scenario environment
      tags: [operations]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Scenario stopped
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StopResponse"
        "404":
          $ref: "#/components/responses/Error"

  /operations/scenarios/{id}/terminal-ticket:
    post:
      operationId: createTerminalTicket
      summary: Create a one-time ticket for a terminal WebSocket
      tags: [operations]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TerminalTicketRequest"
      responses:
        "200":
          description: Short-lived one-time terminal ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TerminalTicketResponse"
        "404":
          $ref: "#/components/responses/Error"

  /operations/scenarios/{id}/terminals/{window}:
    delete:
      operationId: closeTerminalWindow
      summary: Close one terminal tab in the current scenario environment
      tags: [operations]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: window
          in: path
          required: true
          schema:
            type: string
            pattern: "^[a-z][a-z0-9-]{0,31}$"
        - name: node
          in: query
          required: false
          schema:
            type: string
            pattern: "^[a-z][a-z0-9-]{0,31}$"
      responses:
        "200":
          description: Terminal window closed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TerminalWindowCloseResponse"
        "404":
          $ref: "#/components/responses/Error"

  /authoring/sessions:
    post:
      operationId: createAuthoringSession
      summary: Create a human-reviewed scenario authoring session
      tags: [authoring]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Authoring session created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthoringSession"

  /authoring/scenarios/{id}/revisions:
    post:
      operationId: createAuthoringScenarioRevision
      summary: Start a new authoring session for an author-owned Scenario revision
      tags: [authoring]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Scenario revision authoring session created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthoringSession"
        "404":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"

  /authoring/scenarios/{id}/deprecate:
    post:
      operationId: deprecateAuthoringScenario
      summary: Remove an author-owned Scenario from the active Catalog
      tags: [authoring]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Scenario deprecated
        "404":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"

  /authoring/sessions/current:
    get:
      operationId: getCurrentAuthoringSession
      summary: Resume the latest unfinished authoring session for the current user
      tags: [authoring]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Current unfinished authoring session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthoringSession"
        "404":
          $ref: "#/components/responses/Error"

  /authoring/sessions/{id}:
    get:
      operationId: getAuthoringSession
      summary: Read the current authoring revision, conversation, assets, and diff
      tags: [authoring]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AuthoringSessionID"
      responses:
        "200":
          description: Current authoring session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthoringSession"
        "404":
          $ref: "#/components/responses/Error"

  /authoring/sessions/{id}/messages:
    post:
      operationId: sendAuthoringMessage
      summary: Send one natural-language instruction to the authoring agent
      tags: [authoring]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AuthoringSessionID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthoringMessageRequest"
      responses:
        "200":
          description: Server-sent authoring ready, delta, complete, and error events
          content:
            text/event-stream:
              schema:
                type: string
        "202":
          description: The idempotency key already owns an existing authoring run; read the session to obtain its durable result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthoringRunReceipt"
        "404":
          $ref: "#/components/responses/Error"

  /generator/plans:
    post:
      operationId: setGenerationPlan
      summary: Create or revise one user-owned generation plan
      tags: [generator]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratorPlanRequest"
      responses:
        "200":
          description: Persisted plan revision
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorPlanResponse"
        "409":
          $ref: "#/components/responses/Error"

  /generator/workflows:
    get:
      operationId: listActiveGenerations
      summary: List the authenticated user's unfinished generation workflows
      tags: [generator]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Unfinished generation workflows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorWorkflowList"
    post:
      operationId: confirmGeneration
      summary: Confirm one persisted plan revision and create its workflow
      tags: [generator]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratorGenerationConfirmationRequest"
      responses:
        "200":
          description: Generation workflow created or returned from its idempotency receipt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorWorkflow"
        "409":
          $ref: "#/components/responses/Error"

  /generator/workflows/{workflow_id}:
    get:
      operationId: getGeneration
      summary: Read one user-owned workflow and its safe candidate review projection
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
      responses:
        "200":
          description: Generator workflow and current candidate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorGeneration"
        "404":
          $ref: "#/components/responses/Error"

  /generator/workflows/{workflow_id}/review-bundle:
    get:
      operationId: getGeneratorReviewBundle
      summary: Download one immutable author review bundle
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
        - name: kind
          in: query
          required: true
          schema:
            type: string
            enum: [content]
      responses:
        "200":
          description: Version-bound review manifest and deterministic tar.gz payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorReviewBundle"
        "404":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"

  /generator/workflows/{workflow_id}/workspace/turn:
    post:
      operationId: startGeneratorWorkspaceTurn
      summary: Bind one explicit generator turn to a workflow workspace
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratorWorkspaceTurnRequest"
      responses:
        "200":
          description: Workspace turn is bound
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorWorkspaceTurn"
        "409":
          $ref: "#/components/responses/Error"

  /generator/workflows/{workflow_id}/workspace/turn/end:
    post:
      operationId: endGeneratorWorkspaceTurn
      summary: Release one explicit generator workspace turn
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratorWorkspaceTurnRequest"
      responses:
        "204":
          description: Workspace turn released
        "409":
          $ref: "#/components/responses/Error"

  /generator/workflows/{workflow_id}/workspace/files:
    get:
      operationId: listGeneratorWorkspaceFiles
      summary: List files in one bound generator workspace turn
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
        - $ref: "#/components/parameters/GeneratorTurnID"
      responses:
        "200":
          description: Safe relative workspace file entries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorWorkspaceFileList"
        "409":
          $ref: "#/components/responses/Error"
    put:
      operationId: writeGeneratorWorkspaceFile
      summary: Write one relative file in a bound generator workspace turn
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratorWorkspaceFileWriteRequest"
      responses:
        "204":
          description: Workspace file written
        "409":
          $ref: "#/components/responses/Error"

  /generator/workflows/{workflow_id}/workspace/file:
    get:
      operationId: readGeneratorWorkspaceFile
      summary: Read a relative file from one bound generator workspace turn
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
        - $ref: "#/components/parameters/GeneratorTurnID"
        - name: path
          in: query
          required: true
          schema:
            type: string
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 1
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          description: Selected workspace file content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorWorkspaceFileRead"
        "409":
          $ref: "#/components/responses/Error"

  /generator/workflows/{workflow_id}/workspace/commands:
    post:
      operationId: runGeneratorWorkspaceCommand
      summary: Run one command inside a bound generator workspace turn
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratorWorkspaceCommandRequest"
      responses:
        "200":
          description: Command exit code and output
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorWorkspaceCommandResult"
        "409":
          $ref: "#/components/responses/Error"

  /generator/workflows/{workflow_id}/candidate:
    post:
      operationId: submitGeneratorCandidate
      summary: Submit the archive from one bound workspace turn as an immutable candidate
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratorCandidateSubmissionRequest"
      responses:
        "200":
          description: Candidate submission accepted or returned from its idempotency receipt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorGeneration"
        "409":
          $ref: "#/components/responses/Error"

  /generator/workflows/{workflow_id}/content/confirm:
    post:
      operationId: confirmGeneratorContent
      summary: Confirm a verified candidate and begin publication
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratorContentConfirmationRequest"
      responses:
        "200":
          description: Workflow advanced to scenario publication
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorWorkflow"
        "409":
          $ref: "#/components/responses/Error"

  /generator/workflows/{workflow_id}/content/changes:
    post:
      operationId: requestGeneratorContentChanges
      summary: Return a reviewed candidate to generating with user feedback
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratorContentChangeRequest"
      responses:
        "200":
          description: Workflow returned to generating
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorWorkflow"
        "409":
          $ref: "#/components/responses/Error"

  /generator/workflows/{workflow_id}/cancel:
    post:
      operationId: cancelGeneration
      summary: Cancel one unfinished generation workflow
      tags: [generator]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/GeneratorWorkflowID"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeneratorCancellationRequest"
      responses:
        "200":
          description: Workflow cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeneratorWorkflow"
        "409":
          $ref: "#/components/responses/Error"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  responses:
    Error:
      description: Error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"

  parameters:
    AuthoringSessionID:
      name: id
      in: path
      required: true
      schema:
        type: string
    GeneratorWorkflowID:
      name: workflow_id
      in: path
      required: true
      schema:
        type: string
    GeneratorTurnID:
      name: turn_id
      in: query
      required: true
      schema:
        type: string

  schemas:
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string

    DocumentationLink:
      type: object
      required: [key, title, url, embed]
      properties:
        key:
          type: string
          description: Server-generated stable identifier; renaming a link never changes it
        title:
          type: string
        url:
          type: string
        embed:
          type: boolean
          description: Whether the target is known to allow iframe embedding; false renders an external-link card instead

    DocumentationLinkList:
      type: object
      required: [links]
      properties:
        links:
          type: array
          items:
            $ref: "#/components/schemas/DocumentationLink"

    AdminDocumentationLinkInput:
      type: object
      required: [title, url, embed]
      properties:
        title:
          type: string
          minLength: 1
        url:
          type: string
        embed:
          type: boolean

    AdminDocumentationLinkDeletion:
      type: object
      required: [key]
      properties:
        key:
          type: string

    PlaygroundEnvironment:
      type: object
      required: [state]
      properties:
        state:
          type: string
          enum: [none, creating, ready, failed]
          description: Session state; lifecycle reclamation reads as none
        environment_id:
          type: string
          description: Environment UID once a session exists
        runtime:
          type: string
          description: Runtime kind, for example k8s
        generation:
          type: integer
          format: int64
          minimum: 0
          description: Reset generation once a session exists; it increments with every adopted reset, so a ready observed after the generation moved belongs to the wiped environment

    PlaygroundCloseResponse:
      type: object
      required: [closed]
      properties:
        closed:
          type: boolean

    RegisterRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
          minLength: 2
        password:
          type: string
          minLength: 6

    RegisterResponse:
      type: object
      required: [totp_secret, totp_url]
      properties:
        totp_secret:
          type: string
        totp_url:
          type: string

    AdminUser:
      type: object
      required: [id, subject, name, role, created_at]
      properties:
        id:
          type: string
        subject:
          type: string
        name:
          type: string
        role:
          type: string
          enum: [user, admin]
        created_at:
          type: string
          format: date-time

    AdminUserList:
      type: object
      required: [users]
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/AdminUser"

    AdminTOTPResetRequest:
      type: object
      required: [password]
      properties:
        password:
          type: string
          description: The calling admin's own password, confirmed before the target's secret is replaced

    AdminHumanAction:
      type: object
      required: [id, user_id, action, target_type, target_id, detail, created_at]
      properties:
        id:
          type: string
        user_id:
          type: string
          description: The acting user
        action:
          type: string
          enum: [user.totp.reset, environment.release]
        target_type:
          type: string
        target_id:
          type: string
        detail:
          type: object
          additionalProperties: true
        created_at:
          type: string
          format: date-time

    AdminAuditPage:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AdminHumanAction"
        next_cursor:
          type: string
          description: Present when another page exists

    AdminRunnableActionSummary:
      type: object
      required: [by_state, by_attempt]
      properties:
        by_state:
          type: object
          description: Queue counts keyed by state
          additionalProperties:
            type: integer
        by_attempt:
          type: object
          description: Queue counts keyed by retry attempt
          additionalProperties:
            type: integer

    AdminRunnableActionItem:
      type: object
      required: [action_key, content_kind, content_id, content_revision, phase, state, attempt, next_run_at, failure_class, failure_code, failure_summary, flag]
      properties:
        action_key:
          type: string
        content_kind:
          type: string
        content_id:
          type: string
        content_revision:
          type: string
        phase:
          type: string
        state:
          type: string
        attempt:
          type: integer
        lease_expires_at:
          type: string
          format: date-time
          nullable: true
        next_run_at:
          type: string
          format: date-time
        failure_class:
          type: string
        failure_code:
          type: string
        failure_summary:
          type: string
        flag:
          type: string
          enum: [attempt-high, ""]

    AdminRunnableActionPage:
      type: object
      required: [summary, items]
      properties:
        summary:
          $ref: "#/components/schemas/AdminRunnableActionSummary"
        items:
          type: array
          items:
            $ref: "#/components/schemas/AdminRunnableActionItem"

    AdminRunnableReap:
      type: object
      required: [reap_key, state, attempt, last_error, next_attempt_at, updated_at]
      properties:
        reap_key:
          type: string
        state:
          type: string
          enum: [queued, claimed, succeeded]
        attempt:
          type: integer
        last_error:
          type: string
        next_attempt_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    AdminRunnableReapList:
      type: object
      required: [reaps]
      properties:
        reaps:
          type: array
          items:
            $ref: "#/components/schemas/AdminRunnableReap"

    AdminEnvironment:
      type: object
      required: [name, namespace, phase, purpose, created_at]
      properties:
        name:
          type: string
        namespace:
          type: string
        phase:
          type: string
          description: Controller phase, e.g. Pending, Ready, Draining, Failed, Released
        purpose:
          type: string
          enum: [learning, verification]
        user:
          type: string
          description: Owning user identifier from the environment labels
        content_kind:
          type: string
        content_id:
          type: string
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
          nullable: true
          description: Controller-projected lifecycle expiry
        failure:
          $ref: "#/components/schemas/AdminEnvironmentFailure"

    AdminEnvironmentFailure:
      type: object
      required: [class, component, reason, at]
      properties:
        class:
          type: string
          enum: [artifact, infrastructure]
        component:
          type: string
        reason:
          type: string
        message:
          type: string
        at:
          type: string
          format: date-time

    AdminEnvironmentList:
      type: object
      required: [environments]
      properties:
        environments:
          type: array
          items:
            $ref: "#/components/schemas/AdminEnvironment"

    AdminEnvironmentRelease:
      type: object
      required: [id, phase]
      properties:
        id:
          type: string
        phase:
          type: string

    AdminBackgroundService:
      type: object
      required: [name, started_at, last_error]
      properties:
        name:
          type: string
        started_at:
          type: string
          format: date-time
        last_tick_at:
          type: string
          format: date-time
          nullable: true
        last_error:
          type: string

    AdminCatalogIntegrity:
      type: object
      required: [state]
      properties:
        state:
          type: string
          enum: [ok, failed]
        detail:
          type: string
          description: When failed, the integrity error including the offending revision

    AdminSystemStatus:
      type: object
      required: [version, commit, build_time, catalog_integrity, services, playground_max_active]
      properties:
        version:
          type: string
        commit:
          type: string
        build_time:
          type: string
        catalog_release_reference:
          type: string
          nullable: true
        catalog_integrity:
          $ref: "#/components/schemas/AdminCatalogIntegrity"
        playground_max_active:
          type: integer
          description: Configured site-wide cap on concurrently active playground sessions
        services:
          type: array
          items:
            $ref: "#/components/schemas/AdminBackgroundService"

    LoginRequest:
      type: object
      required: [username, password, totp_code]
      properties:
        username:
          type: string
        password:
          type: string
        totp_code:
          type: string

    LoginResponse:
      type: object
      required: [token, user_id, name]
      properties:
        token:
          type: string
          description: JWT access token
        user_id:
          type: string
        name:
          type: string

    MySpaceScenario:
      type: object
      required: [id, title, runtime]
      properties:
        id:
          type: string
        title:
          type: string
        runtime:
          type: string
          enum: [node, k8s]
    MySpaceProfile:
      type: object
      required: [id, name, created_at]
      properties:
        id:
          type: string
        name:
          type: string
        created_at:
          type: string
          format: date-time

    MySpaceEnvironmentQuota:
      type: object
      required: [occupied, maximum]
      properties:
        occupied:
          type: integer
          minimum: 0
        maximum:
          type: integer
          minimum: 0
          nullable: true

    MySpaceSummary:
      type: object
      required: [completed_count, attempted_count, in_progress_environment_count, terminal_learning_seconds, authoring_count, published_count, environment_quota]
      properties:
        completed_count:
          type: integer
          minimum: 0
        attempted_count:
          type: integer
          minimum: 0
        in_progress_environment_count:
          type: integer
          minimum: 0
        terminal_learning_seconds:
          type: integer
          minimum: 0
        authoring_count:
          type: integer
          minimum: 0
        published_count:
          type: integer
          minimum: 0
        environment_quota:
          $ref: "#/components/schemas/MySpaceEnvironmentQuota"

    MySpaceActiveEnvironment:
      type: object
      required: [environment_id, kind, runtime, phase]
      properties:
        environment_id:
          type: string
        kind:
          type: string
          enum: [playground, operations]
          # oapi-codegen shortens enum constant names globally when they stop
          # colliding; pin the qualified names so removing unrelated enums
          # cannot rename these constants under us.
          x-enum-varnames: [MySpaceActiveEnvironmentKindPlayground, MySpaceActiveEnvironmentKindOperations]
          description: playground entries carry no scenario identity or checkpoint progress
        scenario:
          $ref: "#/components/schemas/MySpaceScenario"
        runtime:
          type: string
          enum: [node, k8s]
        phase:
          type: string
        operation:
          type: string
          enum: [None, Resetting]
          description: present when a lifecycle operation is in flight; a Resetting playground row renders as resetting rather than its stale ready phase
        checkpoint_progress:
          $ref: "#/components/schemas/CheckpointProgressSummary"
        expires_at:
          type: string
          format: date-time
          nullable: true

    MySpaceLearningHistory:
      type: object
      required: [scenario, ready_at, learning_seconds, state, checkpoint_first_passes]
      properties:
        scenario:
          $ref: "#/components/schemas/MySpaceScenario"
        ready_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
          nullable: true
        learning_seconds:
          type: integer
          minimum: 0
        state:
          type: string
          enum: [active, completed, stopped, reset, expired]
        checkpoint_first_passes:
          type: array
          items:
            $ref: "#/components/schemas/CheckpointFirstPass"

    MySpaceLearningPage:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/MySpaceLearningHistory"
        next_cursor:
          type: string
          nullable: true

    MySpaceAuthoringDraft:
      type: object
      required: [session_id, title, state, updated_at]
      properties:
        session_id:
          type: string
        title:
          type: string
        state:
          type: string
          enum: [DraftConversation, IntentReview]
        updated_at:
          type: string
          format: date-time

    MySpacePublishedScenario:
      type: object
      required: [scenario, revision_id, state, published_at, attempted_users, completed_users]
      properties:
        scenario:
          $ref: "#/components/schemas/MySpaceScenario"
        revision_id:
          type: string
        state:
          type: string
          enum: [active, deprecated]
        published_at:
          type: string
          format: date-time
        attempted_users:
          type: integer
          minimum: 0
        completed_users:
          type: integer
          minimum: 0
        pass_rate:
          type: number
          format: float
          nullable: true

    MySpaceAuthoring:
      type: object
      required: [drafts, published]
      properties:
        drafts:
          type: array
          items:
            $ref: "#/components/schemas/MySpaceAuthoringDraft"
        published:
          type: array
          items:
            $ref: "#/components/schemas/MySpacePublishedScenario"

    MySpace:
      type: object
      required: [profile, summary, active_environments, recent_learning, recent_learning_next_cursor, authoring]
      properties:
        profile:
          $ref: "#/components/schemas/MySpaceProfile"
        summary:
          $ref: "#/components/schemas/MySpaceSummary"
        active_environments:
          type: array
          items:
            $ref: "#/components/schemas/MySpaceActiveEnvironment"
        recent_learning:
          type: array
          items:
            $ref: "#/components/schemas/MySpaceLearningHistory"
        recent_learning_next_cursor:
          type: string
          nullable: true
        authoring:
          $ref: "#/components/schemas/MySpaceAuthoring"

    ScenarioSummary:
      type: object
      required: [id, title, runtime, scenario_tags, description, published_at]
      properties:
        id:
          type: string
        title:
          type: string
        runtime:
          type: string
          enum: [node, k8s]
        scenario_tags:
          type: array
          items:
            type: string
        description:
          type: string
        published_at:
          type: string
          format: date-time
        solved:
          type: boolean
        active:
          type: boolean
        progress:
          $ref: "#/components/schemas/CheckpointProgressSummary"

    CheckpointProgressSummary:
      type: object
      required: [passed, total]
      properties:
        passed:
          type: integer
          minimum: 0
        total:
          type: integer
          minimum: 0

    ScenarioList:
      type: object
      required: [scenarios]
      properties:
        scenarios:
          type: array
          items:
            $ref: "#/components/schemas/ScenarioSummary"

    ScenarioCheckpoint:
      type: object
      required: [id, title, description]
      properties:
        id:
          type: string
        title:
          type: string
        description:
          type: string
        hint:
          type: string
        node:
          type: string

    ScenarioNode:
      type: object
      required: [name, title]
      properties:
        name:
          type: string
        title:
          type: string

    ScenarioVersion:
      type: object
      required: [component, version]
      properties:
        component:
          type: string
        version:
          type: string

    ScenarioReproductionEvidence:
      type: object
      required: [id, description]
      properties:
        id:
          type: string
        description:
          type: string
        node:
          type: string

    CheckpointResult:
      type: object
      required: [id, passed, summary]
      properties:
        id:
          type: string
        passed:
          type: boolean
        first_passed_at:
          type: string
          format: date-time
          nullable: true
        summary:
          type: string
        details:
          type: string

    CheckpointFirstPass:
      type: object
      required: [checkpoint_id, first_passed_at, summary]
      properties:
        checkpoint_id:
          type: string
        first_passed_at:
          type: string
          format: date-time
        summary:
          type: string

    ScenarioContent:
      type: object
      required: [id, title, description, runtime, scenario_tags, nodes, versions, topology, initialization, reproduction_objective, reproduction_evidence, hints, checkpoints]
      properties:
        id:
          type: string
        title:
          type: string
        description:
          type: string
        runtime:
          type: string
          enum: [node, k8s]
        scenario_tags:
          type: array
          items:
            type: string
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/ScenarioNode"
        versions:
          type: array
          items:
            $ref: "#/components/schemas/ScenarioVersion"
        topology:
          type: string
        initialization:
          type: string
        reproduction_objective:
          type: string
        reproduction_evidence:
          type: array
          items:
            $ref: "#/components/schemas/ScenarioReproductionEvidence"
        problem:
          type: string
          nullable: true
        solution:
          type: string
          nullable: true
        hints:
          type: object
          additionalProperties:
            type: string
        checkpoints:
          type: array
          items:
            $ref: "#/components/schemas/ScenarioCheckpoint"
    ScenarioProgress:
      type: object
      required: [checks]
      properties:
        checks:
          type: array
          items:
            $ref: "#/components/schemas/CheckpointResult"

    AssistantEvidence:
      type: object
      required: [kind, label]
      properties:
        kind:
          type: string
        label:
          type: string

    AssistantMessage:
      type: object
      required: [id, role, content, created_at]
      properties:
        id:
          type: string
        role:
          type: string
          enum: [user, assistant]
        content:
          type: string
        evidence:
          type: array
          items:
            $ref: "#/components/schemas/AssistantEvidence"
        created_at:
          type: string

    AssistantConversation:
      type: object
      required: [id, scenario_id, messages]
      properties:
        id:
          type: string
        scenario_id:
          type: string
        messages:
          type: array
          items:
            $ref: "#/components/schemas/AssistantMessage"
    AssistantTerminalContext:
      type: object
      required: [windows]
      properties:
        node:
          type: string
          pattern: "^[a-z][a-z0-9-]{0,31}$"
        windows:
          type: array
          items:
            type: string
            pattern: "^[a-z][a-z0-9-]{0,31}$"

    AssistantMessageRequest:
      type: object
      required: [content, current_window, terminals]
      properties:
        content:
          type: string
        current_node:
          type: string
          pattern: "^[a-z][a-z0-9-]{0,31}$"
        current_window:
          type: string
          pattern: "^[a-z][a-z0-9-]{0,31}$"
        terminals:
          type: array
          items:
            $ref: "#/components/schemas/AssistantTerminalContext"

    StartResponse:
      type: object
      required: [scenario_title]
      properties:
        scenario_title:
          type: string

    ResetResponse:
      type: object
      required: [scenario_title]
      properties:
        scenario_title:
          type: string
    StopResponse:
      type: object
      required: [stopped, scenario_title]
      properties:
        stopped:
          type: boolean
        scenario_title:
          type: string

    TerminalWindowCloseResponse:
      type: object
      required: [closed]
      properties:
        closed:
          type: boolean

    TerminalTicketRequest:
      type: object
      required: [window]
      properties:
        window:
          type: string
          pattern: "^[a-z][a-z0-9-]{0,31}$"
        node:
          type: string
          pattern: "^[a-z][a-z0-9-]{0,31}$"

    TerminalTicketResponse:
      type: object
      required: [ticket]
      properties:
        ticket:
          type: string

    AuthoringMetadata:
      type: object
      required:
        - title
        - description
        - runtime
      properties:
        title:
          type: string
        description:
          type: string
        runtime:
          type: string
          enum: [node, k8s]

    AuthoringCheckpoint:
      type: object
      required: [id, title, markdown, position]
      properties:
        id:
          type: string
        title:
          type: string
        markdown:
          type: string
        position:
          type: integer

    AuthoringPlan:
      type: object
      required: [metadata, overview, checkpoints]
      properties:
        metadata:
          $ref: "#/components/schemas/AuthoringMetadata"
        overview:
          type: string
        checkpoints:
          type: array
          items:
            $ref: "#/components/schemas/AuthoringCheckpoint"

    VerifiedCheckpoint:
      type: object
      required: [id, title, description]
      properties:
        id:
          type: string
        title:
          type: string
        description:
          type: string
        hint:
          type: string
        node:
          type: string

    VerifiedScenario:
      type: object
      required: [metadata, checkpoints]
      properties:
        metadata:
          $ref: "#/components/schemas/AuthoringMetadata"
        checkpoints:
          type: array
          items:
            $ref: "#/components/schemas/VerifiedCheckpoint"

    AuthoringCandidate:
      type: object
      required: [id, archive_digest, content_revision]
      properties:
        id:
          type: string
        archive_digest:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        content_revision:
          type: string
        failure:
          $ref: "#/components/schemas/AuthoringCandidateFailure"

    AuthoringCandidateFailure:
      type: object
      required: [class, code, summary]
      properties:
        class:
          type: string
          enum: [artifact, infrastructure, cancelled]
        code:
          type: string
        summary:
          type: string

    GeneratorWorkflow:
      type: object
      required: [id, session_id, plan_revision, state, state_version, created_at, updated_at]
      properties:
        id:
          type: string
        session_id:
          type: string
        plan_revision:
          type: integer
          format: int64
          minimum: 0
        state:
          type: string
          enum: [Generating, Judging, MaterializingArtifact, Verifying, NeedsAuthorReview, Publishing, Published, Failed, Cancelled]
        state_version:
          type: integer
          format: int64
          minimum: 1
        candidate_revision_id:
          type: string
        last_error:
          type: string
          nullable: true
        finalizer_error_category:
          type: string
          enum: [deterministic, transient]
          nullable: true
        finalizer_last_error:
          type: string
          nullable: true
        finalizer_last_attempted_at:
          type: string
          format: date-time
          nullable: true
        finalizer_next_retry_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    AuthoringChange:
      type: object
      required: [kind, summary, revision]
      properties:
        kind:
          type: string
        summary:
          type: string
        revision:
          type: integer

    AuthoringMessage:
      type: object
      required: [id, role, content, created_at]
      properties:
        id:
          type: string
        role:
          type: string
          enum: [user, agent, system, event]
        content:
          type: string
        changes:
          type: array
          items:
            $ref: "#/components/schemas/AuthoringChange"
        event:
          $ref: "#/components/schemas/AuthoringRunEvent"
        created_at:
          type: string
          format: date-time

    AuthoringRunEvent:
      type: object
      required: [schema_version, kind, run_id, reason, resumable, recovery]
      properties:
        schema_version:
          type: integer
          enum: [1]
        kind:
          type: string
          enum: [authoring_run_interrupted, authoring_run_failed]
        run_id:
          type: string
        reason:
          type: string
          enum: [deadline_exceeded, server_stopping, server_restarted, permanent_executor_error]
        resumable:
          type: boolean
        recovery:
          type: string
          enum: [workspace, snapshot, candidate, empty]

    AuthoringAsset:
      type: object
      required: [path, content]
      properties:
        path:
          type: string
        content:
          type: string

    AuthoringFileDiff:
      type: object
      required: [path, diff]
      properties:
        path:
          type: string
        diff:
          type: string

    AuthoringMessageRequest:
      type: object
      required: [content, idempotency_key]
      properties:
        content:
          type: string
        idempotency_key:
          type: string
          minLength: 1
          maxLength: 200

    AuthoringRunReceipt:
      type: object
      required: [run_id, status]
      properties:
        run_id:
          type: string
        status:
          type: string
          enum: [running, succeeded, failed, cancelled, interrupted]

    GeneratorPlanRequest:
      type: object
      required: [expected_revision, idempotency_key, plan]
      properties:
        session_id:
          type: string
        expected_revision:
          type: integer
          format: int64
          minimum: 0
        idempotency_key:
          type: string
          minLength: 1
          maxLength: 200
        plan:
          $ref: "#/components/schemas/AuthoringPlan"

    GeneratorPlanResponse:
      type: object
      required: [session_id, plan_revision, plan]
      properties:
        session_id:
          type: string
        plan_revision:
          type: integer
          format: int64
          minimum: 1
        plan:
          $ref: "#/components/schemas/AuthoringPlan"

    GeneratorGenerationConfirmationRequest:
      type: object
      required: [session_id, plan_revision, idempotency_key]
      properties:
        session_id:
          type: string
        plan_revision:
          type: integer
          format: int64
          minimum: 1
        idempotency_key:
          type: string
          minLength: 1
          maxLength: 200

    GeneratorWorkflowList:
      type: object
      required: [workflows]
      properties:
        workflows:
          type: array
          items:
            $ref: "#/components/schemas/GeneratorWorkflow"

    GeneratorGeneration:
      type: object
      required: [workflow, assets, diff]
      properties:
        workflow:
          $ref: "#/components/schemas/GeneratorWorkflow"
        candidate:
          $ref: "#/components/schemas/AuthoringCandidate"
        verified:
          $ref: "#/components/schemas/VerifiedScenario"
        assets:
          type: array
          items:
            $ref: "#/components/schemas/AuthoringAsset"
        diff:
          type: array
          items:
            $ref: "#/components/schemas/AuthoringFileDiff"

    GeneratorReviewBundle:
      type: object
      required: [manifest, payload]
      properties:
        manifest:
          $ref: "#/components/schemas/GeneratorReviewManifest"
        payload:
          type: string
          description: Base64-encoded deterministic tar.gz review payload. The payload never contains the manifest.

    GeneratorReviewManifest:
      type: object
      required: [schema_version, kind, workflow_id, workflow_state, candidate_revision_id, candidate_archive_digest, payload_sha256, exported_at]
      properties:
        schema_version:
          type: integer
          minimum: 1
        kind:
          type: string
          enum: [content]
        workflow_id:
          type: string
        workflow_state:
          type: string
        candidate_revision_id:
          type: string
        candidate_archive_digest:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        payload_sha256:
          type: string
          pattern: '^sha256:[0-9a-f]{64}$'
        exported_at:
          type: string
          format: date-time

    GeneratorWorkspaceTurn:
      type: object
      required: [workflow_id, turn_id]
      properties:
        workflow_id:
          type: string
        turn_id:
          type: string

    GeneratorWorkspaceTurnRequest:
      type: object
      required: [turn_id]
      properties:
        turn_id:
          type: string

    GeneratorWorkspaceFile:
      type: object
      required: [path, directory, size]
      properties:
        path:
          type: string
        directory:
          type: boolean
        size:
          type: integer
          format: int64
          minimum: 0

    GeneratorWorkspaceFileList:
      type: object
      required: [workflow_id, turn_id, files]
      properties:
        workflow_id:
          type: string
        turn_id:
          type: string
        files:
          type: array
          items:
            $ref: "#/components/schemas/GeneratorWorkspaceFile"

    GeneratorWorkspaceFileRead:
      type: object
      required: [workflow_id, turn_id, path, content]
      properties:
        workflow_id:
          type: string
        turn_id:
          type: string
        path:
          type: string
        content:
          type: string

    GeneratorWorkspaceFileWriteRequest:
      type: object
      required: [turn_id, path, content]
      properties:
        turn_id:
          type: string
        path:
          type: string
        content:
          type: string

    GeneratorWorkspaceCommandRequest:
      type: object
      required: [turn_id, command]
      properties:
        turn_id:
          type: string
        command:
          type: string

    GeneratorWorkspaceCommandResult:
      type: object
      required: [workflow_id, turn_id, status]
      properties:
        workflow_id:
          type: string
        turn_id:
          type: string
        status:
          type: string
          enum: [succeeded, failed, unknown]
        exit_code:
          type: integer
        output:
          type: string
        error:
          type: string

    GeneratorCandidateSubmissionRequest:
      type: object
      required: [turn_id, idempotency_key]
      properties:
        turn_id:
          type: string
        idempotency_key:
          type: string
          minLength: 1
          maxLength: 200

    GeneratorContentConfirmationRequest:
      type: object
      required: [candidate_revision_id, idempotency_key]
      properties:
        candidate_revision_id:
          type: string
        idempotency_key:
          type: string
          minLength: 1
          maxLength: 200

    GeneratorContentChangeRequest:
      type: object
      required: [candidate_revision_id, feedback, idempotency_key]
      properties:
        candidate_revision_id:
          type: string
        feedback:
          type: string
          minLength: 1
        idempotency_key:
          type: string
          minLength: 1
          maxLength: 200

    GeneratorCancellationRequest:
      type: object
      required: [idempotency_key]
      properties:
        idempotency_key:
          type: string
          minLength: 1
          maxLength: 200

    AuthoringSession:
      type: object
      required: [id, state, authoring_turn_active, intent_revision, visible_revision, updated_at, intent, messages, workflows]
      properties:
        id:
          type: string
        state:
          type: string
          enum: [DraftConversation, IntentReview, Published]
        authoring_turn_active:
          type: boolean
          description: Whether the durable authoring Agent Run is pending or running. While true, the plan is being updated privately and author actions are unavailable.
        intent_revision:
          type: integer
        visible_revision:
          type: integer
        publish_scenario_id:
          type: string
        revision_scenario_id:
          type: string
        revision_base_active_revision_id:
          type: string
        workflows:
          type: array
          items:
            $ref: "#/components/schemas/GeneratorWorkflow"
        last_error:
          type: string
        updated_at:
          type: string
          format: date-time
        intent:
          $ref: "#/components/schemas/AuthoringPlan"
        messages:
          type: array
          items:
            $ref: "#/components/schemas/AuthoringMessage"
