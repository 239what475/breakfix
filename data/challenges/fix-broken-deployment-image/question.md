# 修复错误的 Deployment 镜像

你现在位于一个已经配置好 `kubectl` 的工作容器中，连接到当前题目的独立 Kubernetes 环境。

`default` 命名空间里已经存在一个名为 `web` 的 Deployment 和对应的 Service，但当前服务不可用。

请你：

1. 检查 `web` Deployment 的状态
2. 找出它无法正常运行的原因
3. 直接在集群中修复它，让 Deployment 恢复可用

完成后直接提交即可。
