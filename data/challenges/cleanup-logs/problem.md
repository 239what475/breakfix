# 批量压缩旧日志

服务器出现磁盘空间告警。`/var/log` 下堆积了大量历史日志，但现有清理流程没有生效。

编写 `/usr/local/bin/cleanup.sh`，并实际运行它。脚本需要：

1. 找出 `/var/log` 下超过 7 天且大于 100MB 的 `.log` 文件。
2. 将每个目标文件归档到 `/backup`，使用 `.tar.gz` 格式。
3. 不处理仍然较新或体积较小的日志。

可以使用检查点了解当前环境是否满足目标。检查点只读取当前状态，不会替你运行清理脚本。
