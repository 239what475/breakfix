服务器出现磁盘空间告警，排查后发现 `/var/log` 下堆积了大量历史日志文件，但现有清理流程没有生效。

你的任务：

1. 编写 `/usr/local/bin/cleanup.sh`
2. 找出 `/var/log` 下 7 天前且大于 100MB 的 `.log` 文件
3. 将这些文件逐个打包压缩到 `/backup` 目录，格式为 `.tar.gz`
4. 不要误处理仍然较新或体积较小的日志文件

完成后运行你的脚本。提交时平台会验证：

- 目标文件是否都被正确归档
- 不该处理的文件是否仍然保留
- `/backup` 中生成的归档是否有效
