# 下一阶段方向

Breakfix 不再以扩充题库或编排学习 Roadmap 为产品目标。项目聚焦两个彼此独立的方向：**文档实践化**与
**可复现运维现场**。两者面向不同用户、来自不同内容源、采用不同的组织方式，只共享可复现环境的底层设施。

```text
文档实践化                                      可复现运维现场
面向正在阅读文档的人                            面向记录和分享真实经验的工程师
按上游文档结构组织                              按搜索和少量标签组织
内容是依附于段落的可执行示例                    内容是独立的运维现场
                 \                            /
                  环境构建 / 运行 / 终端 / 观测
                  版本固定 / 验证 / 重置 / 回收
```

Roadmap、课程图以及 `Domain -> Topic -> Challenge` 层级不再属于目标产品。项目不替用户规定学习顺序，也不试图维护一套
独立于上游文档的知识关系。

## 文档实践化

技术文档善于准确、完整地说明概念，但读者往往缺少能够立即验证这些概念的环境。文档实践化是在原文的关键位置附加
**可执行示例**：读者无需离开阅读上下文，即可创建临时环境、运行示例、修改配置并观察结果，再回到文档继续阅读。

例如，在 Kubernetes Pod 生命周期文档中，可以分别运行短小示例来观察 Pod phase、容器状态、重启策略、探针结果和终止
流程。示例的目的在于帮助理解对应段落，不要求伪装成故障排查题，也不必具有答案、难度、提示或修复任务。一个示例可以
只是展示状态变化，也可以允许读者完成少量操作并用断言确认文档结论。

文档自身已经提供目录、章节、链接和上下文关系，因此平台不再为它维护 Topic、知识图谱或标签。一个可执行示例只绑定到
明确的上游位置：

- 文档来源及其许可证；
- 上游仓库和固定 revision；
- 页面路径和章节锚点；
- 支持的项目或软件版本；
- 示例自身的不可变 revision。

### 同步与维护

平台应从允许再发布的上游文档仓库同步内容，而不是抓取渲染后的网页。每次同步保存上游 commit、来源链接、许可证与署名
信息；文档经过翻译或改编时明确标注修改。同步流程识别页面新增、修改、移动和删除，并根据页面路径与章节锚点报告失效或
受影响的示例。Logo、图片及第三方嵌入内容不能仅凭仓库根许可证推定可用，必须单独核对或暂不同步。

文档更新不应让 AI 直接改写并发布示例。AI 可以提取适合实践的段落、生成示例初稿和解释，但发布前必须在目标运行时真实
执行，确认现象可观察、断言成立、重置有效，并由维护者审核文档含义与示例是否一致。

文档实践化的首个闭环使用 Kubernetes 官方文档完成：同步一个固定版本的 Pod 生命周期页面，在若干关键段落旁提供可直接
运行的示例，并验证文档升级后能够定位受影响的示例。第一阶段不追求覆盖整个 Kubernetes 文档站。

## 可复现运维现场

可复现运维现场用于沉淀真实工程经验。工程师遇到值得保留的问题后，可以将经过脱敏的环境定义、软件版本、触发条件、症状
和关键证据发布出来，让其他人重新进入同一个现场进行观察、诊断或修复。

现场是独立内容，不要求依附文档，也不必改造成有唯一答案的练习题。贡献者可以选择提供诊断思路、修复方法和自动验证，
但最低发布标准是：

1. 从声明的干净基线可以确定性地建立环境并复现目标现象；
2. 运行所需的软件、镜像、配置和数据版本明确且可获取；
3. 用户能够通过终端、日志、事件或指标观察到关键证据；
4. 内容不包含真实凭据、个人信息、客户数据或其他未脱敏材料；
5. 环境可以可靠地重置、停止和回收。

运维现场没有文档目录可以继承，因此使用搜索和少量自由标签发现内容。标签只描述便于检索的客观信息，例如技术组件、故障
领域、主要症状和运行要求；不建立受控的 Domain/Topic 层级、前置关系或课程图，也不通过 Agent 维护标签之间的关系。

首个分享闭环应允许工程师创建一个现场、在真实环境中确认复现、预览脱敏后的发布内容、显式发布，并让另一位用户从干净
状态启动相同 revision。平台验证的是“现场能否复现”，不是强制贡献者给出标准答案。

## 共享运行底座

两个方向复用同一套基础能力，但不共享上层内容模型和组织关系：

- 声明并构建隔离的 Node 或 Kubernetes 环境；
- 固定运行时、依赖、镜像与内容 revision；
- 执行初始化步骤并等待目标状态出现；
- 提供浏览器终端以及日志、事件、状态和指标等观测入口；
- 支持可选的断言或检查点，不把检查点强制为所有内容的产品形态；
- 在发布前进行真实构建和运行验证；
- 一键重置、停止、超时回收，并限制权限、网络和资源；
- 保留不可变历史 revision，使已有运行记录不会随内容更新而漂移。

共享底座的核心抽象是“可版本化、可验证、可回收的运行环境”。文档示例和运维现场在它之上分别定义自己的元数据、发布
标准和界面，不能为了复用而重新合并成同一种 Challenge 或学习路线。

## 明确不做

- 不维护学习 Roadmap、课程依赖图或推荐学习顺序；
- 不以批量生成传统题目和答案为内容目标；
- 不为文档重复建立 Topic、标签或知识图谱；
- 不要求每个文档示例都是故障场景，也不要求每个运维现场都是教学练习；
- 不在尚未跑通两个最小闭环前扩展排行榜、证书、讨论区、LMS 或团队管理功能。
