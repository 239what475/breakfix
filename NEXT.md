# 下一阶段方向

Breakfix 的两个产品方向已经落地：运维现场与文档实践是两个独立的一级模块，分别拥有自己的内容模型、审核流程、发布规则和 Catalog，运行在同一套公共 Runnable 底座上；顶部导航由“文档”“运维场景”和“我的空间”组成。两个最小闭环均已验收：工程师可以创建现场、在真实环境中确认复现、显式发布，其他用户从干净状态启动相同 revision；固定版本的 Kubernetes 文档经纯 Agent 流水线规划、双重门禁、真实验证后发布 PracticeRevision。

Roadmap、课程图以及 `Domain -> Topic -> Challenge` 层级不属于目标产品。项目不替用户规定学习顺序，也不试图维护一套独立于上游文档的知识关系。

本文件只记录尚未实现的方向；已完成阶段的任务拆解与验收记录见 git 历史中的 TODO.md。

## 阅读器内的实践体验

PracticeRevision 已经可以发布，实践索引保存了页面/锚点到已发布 revision 的映射，但阅读页尚未把它们呈现给读者。下一阶段补齐这最后一环，第一步是把阅读页本身切到文档库解析产物（Server 提供、digest 校验，替代 iframe 上游渲染 HTML）——读者看到的与流水线据以生成的是同一份字节，实践入口按锚点挂载成为一等公民，docs-site nginx 与 iframe 同步机制退场（任务拆解见 TODO 阶段三）：

- 按标题锚点在对应段落旁显示实践入口，识别读者当前滚动到的章节；
- 读者不离开阅读上下文即可创建临时环境、运行示例、修改配置并观察结果；
- 实践终端与阅读页联动，完成后回到原文继续阅读。

可执行示例的设计原则不变：示例依附段落、帮助理解对应概念，不伪装成故障排查题，不要求答案、难度、提示或修复任务；一个示例可以只展示状态变化，也可以允许读者完成少量操作并用断言确认文档结论。

## 文档工作流：切库与多页铺开

文档内容输入尚未切换到离线文档库：Server 仍在运行时解析渲染 HTML，生产装配仍依赖 `source_root`/`snapshot_root` 挂载，部署物理上只携带一个钉死页面（ConfigMap）。后续方向：

- Server 文档 Reader 改读文档库并校验 digest，移除运行时 HTML 解析路径与源码读取能力；
- 文档库以只读卷（或镜像层）进入部署——854 页产物加资产约数十 MB，超出 ConfigMap 1MiB 上限，不用 ConfigMap；库自包含后 `snapshot_root` 退场，文档库成为 Server 唯一文档输入；
- 流水线证据切换为结构化章节文本（正文 + 围栏代码块）；证据绑定三元组（页面 digest + 解析器版本 + 上游 commit）进入 artifact ledger 与 PublicationManifest；
- docs-smoke 与文档 E2E 的 fixture 换为真实渲染页（注意 ConfigMap 上限，必要时改卷挂载）。

多页铺开：工作流按页面/锚点铺开，解除"单页单锚点"的部署钉死；批次控制（范围选择、并发、限速、暂停/取消/重试失败页）与调度策略（section 优先级、失败重试、限速）待独立设计，状态机、门禁、恢复逻辑复用；runnable action 失败到工作流 Failed 的自动映射看门狗、以及修订循环死代码（ReviseAt/AcquireWorkflowLease）的删除或激活，都随该设计定。多语言维持 2026-09-16"暂不实施"的决定。

## 多文档源扩展

离线库格式是架构的缝：库以下的整栈——Server 读取与 digest 校验、证据三元组、状态机与门禁、发布与 practice 索引、管理台、阅读器——只认库格式，不认上游。新文档源的扩展成本定义在两处，而非流水线：

- 每源一份的 generator：拉上游、（必要时）渲染、提取成库的 markdown 投影、算 digest、建 tree/anchors；Hugo 系可复用现有 extractor，tree 来源因站而异；
- 每域一份的验证环境目录与 runtime profile（k8s 实践跑 kubectl，别的域要自己的镜像与工具链），外加 prompt 领域调优与 license 差异。

纪律：第二个真实文档源出现之前不做 extractor 抽象重构；k8s 特有逻辑（feature_states、侧栏解析）不漏出 generator。多源时的已知小缺口：config 单源钉定（`library_root`/`source_id`）、管理台树与阅读器导航的源维度。

## 运维现场后续

第一阶段不建设复杂的自动脱敏或 DLP 系统的现状保持不变：作者在显式发布前必须能够查看完整发布内容，并对其中不含真实凭据、个人信息和客户数据负责。只有在真实投稿暴露出明确问题时，再增加高置信度的敏感信息拦截规则。

## 明确不做

- 不维护学习 Roadmap、课程依赖图或推荐学习顺序；
- 不以批量生成传统题目和答案为内容目标；
- 不为文档重复建立 Topic、标签或知识图谱；
- 不要求每个文档示例都是故障场景，也不要求每个运维现场都是教学练习；
- 不扩展排行榜、证书、讨论区、LMS 或团队管理功能。
