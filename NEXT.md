# 下一阶段方向

Breakfix 的两个产品方向已经落地：运维现场与文档实践是两个独立的一级模块，分别拥有自己的内容模型、审核流程、发布规则和 Catalog，运行在同一套公共 Runnable 底座上；顶部导航由“文档”“运维场景”和“我的空间”组成。两个最小闭环均已验收：工程师可以创建现场、在真实环境中确认复现、显式发布，其他用户从干净状态启动相同 revision；固定版本的 Kubernetes 文档经纯 Agent 流水线规划、双重门禁、真实验证后发布 PracticeRevision。

Roadmap、课程图以及 `Domain -> Topic -> Challenge` 层级不属于目标产品。项目不替用户规定学习顺序，也不试图维护一套独立于上游文档的知识关系。

本文件只记录尚未实现的方向；已完成阶段的任务拆解与验收记录见 git 历史中的 TODO.md。

## 文档工作流：多页铺开（已立项，拆解见 TODO.md 当前阶段）

文档内容输入已完成切换（2026-09-18，git 历史 TODO 阶段一）：离线文档库是 Server
唯一文档输入，运行时 HTML 解析与源码读取路径已删除，证据绑定三元组进入
ledger 与 PublicationManifest，阅读器与流水线同源。多页铺开已于 2026-09-19
立项定形：批次是范围控制的唯一载体（声明式对象：范围 + 并发策略 + 状态，进
流水线），由 Server 内与既有后台循环同款的调度循环按并发阀值驱动点火，可
暂停/恢复/取消/重试失败页、跨重启续跑；条目默认页级，锚点由确定性规则解析
（显式 (page, anchor) 可覆盖）；runnable action 失败经看门狗自动映射工作流
Failed，修订循环死代码（ReviseAt/AcquireWorkflowLease）删除；全量库以专用
镜像进部署，config 解除单页钉定、保留库身份钉定；管理台"文档"分区按语料
组织（镜像上游树 + 聚合，不建第二套层级）。三个 Reviewing 门禁保持自动
（LLM 评审 + 确定性裁决），不引入人工审批流。多语言维持 2026-09-16"暂不
实施"的决定。

铺开不改变实践内容的设计原则：示例依附段落、帮助理解对应概念，不伪装成
故障排查题，不要求答案、难度、提示或修复任务；一个示例可以只展示状态变化，
也可以允许读者完成少量操作、观察结果印证文档结论。

## 多文档源扩展

离线库格式是架构的缝：库以下的整栈——Server 读取与 digest 校验、证据三元组、状态机与门禁、发布与 practice 索引、管理台、阅读器——只认库格式，不认上游。新文档源的扩展成本定义在两处，而非流水线：

- 每源一份的 generator：拉上游、（必要时）渲染、提取成库的 markdown 投影、算 digest、建 tree/anchors；Hugo 系可复用现有 extractor，tree 来源因站而异；
- 每域一份的验证环境目录与 runtime profile（k8s 实践跑 kubectl，别的域要自己的镜像与工具链），外加 prompt 领域调优与 license 差异。

纪律：第二个真实文档源出现之前不做 extractor 抽象重构；k8s 特有逻辑（feature_states、侧栏解析）不漏出 generator。多源时的已知小缺口：config 单源钉定（`library_root`/`source_id`）、管理台树与阅读器导航的源维度。

## 运维现场后续

第一阶段不建设复杂的自动脱敏或 DLP 系统的现状保持不变：作者在显式发布前必须能够查看完整发布内容，并对其中不含真实凭据、个人信息和客户数据负责。只有在真实投稿暴露出明确问题时，再增加高置信度的敏感信息拦截规则。

## 明确不做

- 不维护学习 Roadmap、课程依赖图或推荐学习顺序；
- 不以批量生成传统题目和答案为内容目标；
- 不为文档重复建立 Topic、标签或知识图谱；
- 不要求每个文档示例都是故障场景，也不要求每个运维现场都是教学练习；
- 不扩展排行榜、证书、讨论区、LMS 或团队管理功能。
