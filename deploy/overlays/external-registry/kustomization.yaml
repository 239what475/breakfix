apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This overlay keeps the complete Breakfix control plane without the bundled
# Registry. registry.address and credentials remain ordinary runtime
# configuration, so Builder/Publisher/Verifier semantics do not change.
resources:
  - ../../base
