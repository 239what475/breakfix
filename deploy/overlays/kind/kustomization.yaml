apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: breakfix-system

# Kustomize cannot import an ancestor directory as a resource. The Kind entry
# therefore contains only its additive Registry resources; scripts/kind/runtime.sh
# applies the root package first and this overlay second.
resources:
  - registry.yaml
