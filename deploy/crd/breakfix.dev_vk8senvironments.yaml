---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: vk8senvironments.breakfix.dev
spec:
  group: breakfix.dev
  names:
    kind: VK8sEnvironment
    listKind: VK8sEnvironmentList
    plural: vk8senvironments
    singular: vk8senvironment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.environment.purpose
      name: Purpose
      type: string
    - jsonPath: .status.environment.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              environment:
                description: |-
                  EnvironmentSpec is shared by both runtime kinds. Source, purpose and
                  checkpoints are immutable; only ActivityAt may be renewed by Server.
                properties:
                  checkpoints:
                    items:
                      properties:
                        id:
                          type: string
                        node:
                          type: string
                      required:
                      - id
                      type: object
                    type: array
                  lifecycle:
                    properties:
                      activityAt:
                        format: date-time
                        type: string
                      deadlineAt:
                        format: date-time
                        type: string
                      drainGracePeriodSeconds:
                        format: int64
                        type: integer
                      idleTtlSeconds:
                        format: int64
                        type: integer
                    type: object
                  purpose:
                    enum:
                    - learning
                    - verification
                    type: string
                  source:
                    properties:
                      kind:
                        enum:
                        - published
                        - candidate
                        type: string
                      ref:
                        type: string
                      revision:
                        type: string
                    required:
                    - kind
                    - ref
                    - revision
                    type: object
                  userRef:
                    type: string
                required:
                - checkpoints
                - lifecycle
                - purpose
                - source
                type: object
              runtime:
                properties:
                  imageDigest:
                    type: string
                  managementTerminalImage:
                    type: string
                  profileRevision:
                    type: string
                  resources:
                    properties:
                      controlPlaneCpu:
                        type: string
                      controlPlaneMemory:
                        type: string
                      quotaCpu:
                        type: string
                      quotaEphemeralStorage:
                        type: string
                      quotaMemory:
                        type: string
                      terminalCpu:
                        type: string
                      terminalMemory:
                        type: string
                    required:
                    - controlPlaneCpu
                    - controlPlaneMemory
                    - quotaCpu
                    - quotaEphemeralStorage
                    - quotaMemory
                    - terminalCpu
                    - terminalMemory
                    type: object
                  version:
                    type: string
                required:
                - imageDigest
                - managementTerminalImage
                - profileRevision
                - resources
                - version
                type: object
            required:
            - environment
            - runtime
            type: object
          status:
            properties:
              environment:
                properties:
                  checkpoints:
                    description: CheckpointStatus is controller-owned state from the
                      most recent checkpoint run.
                    properties:
                      checkedAt:
                        format: date-time
                        type: string
                      error:
                        type: string
                      results:
                        items:
                          properties:
                            details:
                              type: string
                            firstPassedAt:
                              format: date-time
                              type: string
                            id:
                              type: string
                            passed:
                              type: boolean
                            summary:
                              type: string
                          required:
                          - id
                          - passed
                          - summary
                          type: object
                        type: array
                    type: object
                  completedAt:
                    format: date-time
                    type: string
                  conditions:
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    type: array
                  destroyedAt:
                    format: date-time
                    type: string
                  expiresAt:
                    format: date-time
                    type: string
                  failure:
                    properties:
                      at:
                        format: date-time
                        type: string
                      class:
                        enum:
                        - artifact
                        - infrastructure
                        type: string
                      component:
                        type: string
                      message:
                        type: string
                      reason:
                        type: string
                    required:
                    - at
                    - class
                    - component
                    - reason
                    type: object
                  lastActivityAt:
                    format: date-time
                    type: string
                  observedGeneration:
                    format: int64
                    type: integer
                  phase:
                    enum:
                    - Pending
                    - Provisioning
                    - Ready
                    - Draining
                    - Completed
                    - Destroyed
                    - Failed
                    type: string
                  readyAt:
                    format: date-time
                    type: string
                  startedAt:
                    format: date-time
                    type: string
                type: object
              runtime:
                properties:
                  initialized:
                    type: boolean
                  kubeconfigSecretName:
                    type: string
                  namespace:
                    type: string
                  terminalPodName:
                    type: string
                  vclusterName:
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
