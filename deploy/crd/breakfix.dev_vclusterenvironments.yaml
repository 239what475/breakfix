---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: vclusterenvironments.breakfix.dev
spec:
  group: breakfix.dev
  names:
    kind: VClusterEnvironment
    listKind: VClusterEnvironmentList
    plural: vclusterenvironments
    singular: vclusterenvironment
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              activityAt:
                format: date-time
                type: string
              challengeRef:
                type: string
              challengeRevision:
                description: |-
                  These fields stay schema-optional so pre-split Environment CRDs can be
                  finalized after an upgrade. The Controller requires them before provision.
                type: string
              checkpointIDs:
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              cleanupPolicy:
                properties:
                  autoDestroyAfterIdle:
                    type: boolean
                  forceCleanupOnFailure:
                    type: boolean
                type: object
              image:
                type: string
              resources:
                properties:
                  workspaceCpu:
                    type: string
                  workspaceEphemeralStorage:
                    type: string
                  workspaceMemory:
                    type: string
                type: object
              runtime:
                enum:
                - container
                - vcluster
                type: string
              timeouts:
                properties:
                  destroyTimeoutSeconds:
                    format: int64
                    type: integer
                  drainGracePeriodSeconds:
                    format: int64
                    type: integer
                  idleTtlSeconds:
                    format: int64
                    type: integer
                  readyTimeoutSeconds:
                    format: int64
                    type: integer
                type: object
              userRef:
                type: string
              vcluster:
                properties:
                  controlPlaneCpu:
                    type: string
                  controlPlaneMemory:
                    type: string
                  profile:
                    type: string
                  quotaCpu:
                    type: string
                  quotaEphemeralStorage:
                    type: string
                  quotaMemory:
                    type: string
                  version:
                    type: string
                type: object
            required:
            - challengeRef
            - image
            - userRef
            type: object
          status:
            properties:
              checkpoints:
                description: CheckpointStatus is controller-owned state from the most
                  recent checkpoint run.
                properties:
                  checkedAt:
                    format: date-time
                    type: string
                  error:
                    type: string
                  results:
                    items:
                      properties:
                        details:
                          type: string
                        id:
                          type: string
                        passed:
                          type: boolean
                        summary:
                          type: string
                      required:
                      - id
                      - passed
                      - summary
                      type: object
                    type: array
                type: object
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              destroyedAt:
                format: date-time
                type: string
              expiresAt:
                format: date-time
                type: string
              kubeconfigReady:
                type: boolean
              kubeconfigSecretName:
                type: string
              lastActivityAt:
                format: date-time
                type: string
              lastError:
                properties:
                  at:
                    format: date-time
                    type: string
                  code:
                    type: string
                  component:
                    type: string
                  message:
                    type: string
                  retriable:
                    type: boolean
                type: object
              message:
                type: string
              namespace:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Provisioning
                - Ready
                - Draining
                - Completed
                - Destroyed
                - Failed
                type: string
              profile:
                type: string
              readyAt:
                format: date-time
                type: string
              reason:
                type: string
              startedAt:
                format: date-time
                type: string
              vclusterName:
                type: string
              version:
                type: string
              workspacePodName:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
